% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motus.R
\name{sync_motus_receivers}
\alias{sync_motus_receivers}
\title{Synchronize all staged CASSN Motus receiver databases}
\usage{
sync_motus_receivers(staged_dir, update = TRUE)
}
\arguments{
\item{staged_dir}{Path to a staged CASSN folder. It must contain
\code{metadata_inputs/motus.csv} with \code{m_station_id}, \code{receiver_serial}, and
\code{site_short_name} columns.}

\item{update}{If \code{TRUE}, download and merge data available from Motus. If
\code{FALSE}, only inspect databases that already exist in the staged folder.}
}
\value{
A synchronization summary, invisibly, with one row per receiver.
}
\description{
Read receiver serials from the staged CASSN Motus configuration and create
or update one receiver-scoped \code{.motus} SQLite database per configured
receiver. The databases are written to the staged \verb{motus/} folder for use by
\code{\link[=write_occurrence_csv]{write_occurrence_csv()}}.
}
\details{
Motus authentication is handled by the \code{motus} package and may
prompt on the first network call in an R session. This function downloads
the detections and refreshes the tag, species, and receiver metadata used
by the occurrence pipeline. It skips Motus activity, node, and
deprecated-batch data, which the pipeline does not use. Existing databases
are updated in place; missing databases are created when \code{update = TRUE}.
}
\examples{
\dontrun{
summary <- sync_motus_receivers("/path/to/staged_dir")
}
}
